		$(window).scroll(function() {
			$('#left-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-four').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-five').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-six').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-seven').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-eight').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-nine').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#left-ten').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slide-Left");
				}
			});
			
			$('#right-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-four').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-five').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-six').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-seven').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-eight').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-nine').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#right-ten').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+400) {
					$(this).addClass("slideRight");
				}
			});
			
			$('#up-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-four').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-five').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-six').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-seven').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-eight').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-nine').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			$('#up-ten').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			
			$('#up-11').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			
			$('#up-12').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});

			

			
			$('#up-13').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			
			$('#up-14').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			
			$('#up-15').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideUp");
				}
			});
			
			
			$('#down-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideDown");
				}
			});
			
			$('#down-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideDown");
				}
			});
			
			$('#down-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideDown");
				}
			});
			
			$('#down-four').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideDown");
				}
			});
			
			$('#down-five').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("slideDown");
				}
			});			
			
			
			$('#fade-1').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-2').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-3').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-4').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-5').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-6').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-7').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-8').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-9').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-10').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-11').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-12').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-13').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-14').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			$('#fade-15').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("fadeIn");
				}
			});
			
			
			$('#float-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("floating");
				}
			});
			
			
			
			
			$('#big-enter-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-four').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-five').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-six').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-seven').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			$('#big-enter-eight').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("bigEntrance");
				}
			});
			
			
			$('#floating-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("floating");
				}
			});
			
			
			$('#floating-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("floating");
				}
			});
			
			
			$('#floating-three').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("floating");
				}
			});
			
			
			$('#pulse-1').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			
			$('#pulse-2').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			
			$('#pulse-3').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			$('#pulse-4').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			$('#pulse-5').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			$('#pulse-6').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			
			$('#pulse-7').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			$('#pulse-8').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+0) {
					$(this).addClass("pulse");
				}
			});
			
			
			$('#strc-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("stretchRight");
				}
			});
			
			$('#strc-two').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("stretchRight");
				}
			});
			
			$('#tossing-one').each(function(){
			var imagePos = $(this).offset().top;
			
			var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+550) {
					$(this).addClass("tossing");
				}
			});
			
			
			
			
			
			
			
			
			
			
								

		});